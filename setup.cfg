[metadata]
name = web-vuln-scanner
version = 0.1.0
description = A web vulnerability scanner system.
author = Your Name
author_email = your.email@example.com
url = https://github.com/yourusername/web-vuln-scanner
license = MIT

[options]
packages = find:
install_requires =
    Flask
    requests

[options.packages.find]
where = src

[tool:pytest]
testpaths = tests